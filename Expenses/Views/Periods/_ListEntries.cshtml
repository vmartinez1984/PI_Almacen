@model IEnumerable<Expenses.Models.Entry>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateRegister)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateRegister)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" asp-controller="Entries">Editar</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" asp-controller="Entries">Borrar</a>
                </td>
            </tr>
        }
        <tr>
            <td>
            </td>
            <td>
                Total
            </td>
            <td>
                @Model.Sum(x=> x.Amount).ToString("C")
            </td>
            <td>
            </td>
        </tr>
    </tbody>
</table>